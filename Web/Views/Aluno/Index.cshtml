@model IEnumerable<Modelo.DAO.Aluno>

@{
    ViewBag.Title = "Login Aluno";
}

<h2>Login Aluno @ViewBag.aluno</h2>
<!--
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Senha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Curso)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pessoa.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Senha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Curso)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pessoa.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RA }) |
            @Html.ActionLink("Details", "Details", new { id=item.RA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RA })
        </td>
    </tr>
}

</table> -->